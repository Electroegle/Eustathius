# Eustathius runtime dependencies
# Python 3.10+  |  pip install -r requirements.txt

# ── Ollama & HTTP ─────────────────────────────────────────────────────────────
ollama>=0.3,<1.0
aiohttp>=3.9,<4.0
httpx>=0.27,<1.0
aiofiles>=23.0,<25.0

# ── CLI & UI ──────────────────────────────────────────────────────────────────
rich>=13.7,<15.0
prompt_toolkit>=3.0,<4.0

# ── System monitoring ─────────────────────────────────────────────────────────
psutil>=6.0,<7.0

# ── Config ────────────────────────────────────────────────────────────────────
pyyaml>=6.0,<7.0

# ── Vector memory ─────────────────────────────────────────────────────────────
chromadb>=0.5,<1.0
sentence-transformers>=3.0,<4.0

# ── Web / research tools ──────────────────────────────────────────────────────
beautifulsoup4>=4.12,<5.0
lxml>=5.0,<6.0
duckduckgo-search>=6.0,<8.0

# ── Document tools ────────────────────────────────────────────────────────────
python-docx>=1.1,<2.0
PyPDF2>=3.0,<4.0
Pillow>=10.0,<12.0

# ── Voice (optional – omit if you don't use voice commands) ──────────────────
pyttsx3>=2.90,<3.0
SpeechRecognition>=3.10,<4.0
sounddevice>=0.4,<1.0

# ── Scheduling & filesystem helpers ──────────────────────────────────────────
schedule>=1.2,<2.0
watchdog>=4.0,<5.0
gitpython>=3.1,<4.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0,<9.0
